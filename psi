#!/usr/bin/env bash
# psi - interactive process viewer using cmdz

SCRIPT="$0"
[[ -L "$SCRIPT" ]] && SCRIPT="$(readlink "$SCRIPT")"
SCRIPT_DIR="$(cd "$(dirname "$SCRIPT")" && pwd)"

exec "$SCRIPT_DIR/cmdz" "psi-cmd${*:+ $*}" --config "$SCRIPT_DIR/psi.conf"


