#!/usr/bin/env bash
# Handle live updates in command mode

state=$(<"$STATE_FILE")
mode="${state%%|*}"

if [[ "$mode" == "command" ]]; then
    printf 'reload:eval %q 2>/dev/null' "$FZF_QUERY"
fi
# In query mode: output nothing, let fzf filter normally

